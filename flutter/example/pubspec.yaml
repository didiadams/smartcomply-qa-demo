name: smartcomply_example
description: Example app demonstrating the SmartComply Flutter SDK.

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  smartcomply_sdk:
    path: ../

flutter:
  uses-material-design: true
